{% extends 'DashboardTemplate.html.twig' %}

{% block body %}

    <div class="card">
        <div class="card-body">
            <div class="card-title">{{ pet.id }}</div>
            <div class="card-subtitle">{{ pet.name }}</div>
            <div class="card-text">{{ pet.race }}</div>

        </div>
    </div>

{% endblock %}

{% block title %}
{{ pet.name }}
{% endblock %}

